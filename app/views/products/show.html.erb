<h1>Product: <%= @product.name %></h1>

<table border='1px solid'>
  <tr>
    <th>Image</th>
    <th>Description</th>
  </tr>

  <tr>
    <td><%= link_to(image_tag(@product.image.url(:medium)), @product.image.url(:original))%></td>
    <td><%= simple_format @product.description %></td>
  <tr>
<table>
